spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        sumant-input1:
          destination: ssr-input
          contentType: application/json
        sumant-input2:
          destination: ssr-input
          contentType: application/json
        sumant-output:
          destination: ssr-output
          contentType: application/json
          